<?xml version="1.0" encoding="UTF-8"?>
<Destination >
  <!-- We don't use the full Destination element, but only the DestinationDisplayRef within -->
  <DestinationDisplayRef ref="the ref" version="1">
    <!-- We use a DestinationDiplayRef in such cases and nothing else. -->
  </DestinationDisplayRef>
</Destination>
